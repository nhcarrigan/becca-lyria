/**
 * Defines the config keys that are stored as arrays.
 */
export type ArraySettings =
  | "hearts"
  | "blocked"
  | "self_roles"
  | "automod_channels"
  | "no_automod_channels"
  | "automod_roles"
  | "allowed_links"
  | "level_roles"
  | "level_ignore"
  | "emote_channels";
